$kind: http-request
method: PUT
url: '{{base_url}}/comments/{{comment_id}}'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
body:
  type: json
  content: |-
    {
      "content": "Updated comment content."
    }
