$kind: http-request
method: POST
url: '{{base_url}}/urls'
order: 2000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
body:
  type: json
  content: |-
    {
      "url": "https://example.com/article",
      "domain_name": "example.com"
    }
